export interface ICovid {
  active: number;
  confirmed: number;
  deaths: number;
  deathConfirmed: number;
  recovered: number;
  state: string;
  deltaconfirmed: number;
  deltarecovered: number;
  deltadeaths: number;
}
